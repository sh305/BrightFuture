import React from "react"; import {Card,CardContent,Button,Typography,Stack,Chip} from "@mui/material"; import {Link} from "react-router-dom"; import ComputerIcon from "@mui/icons-material/Computer";
export default function CourseCard({course}){return <Card sx={{height:"100%",display:"flex",flexDirection:"column"}}><CardContent sx={{p:3,flexGrow:1}}><ComputerIcon color="primary" sx={{fontSize:45}}/><Typography variant="h5" fontWeight={800} sx={{mt:1}}>{course.name}</Typography><Stack direction="row" spacing={1} sx={{my:2}}><Chip label={course.duration}/><Chip label={course.fee} color="secondary"/></Stack><Typography color="text.secondary">{course.desc}</Typography></CardContent><Button component={Link} to={`/courses/${course.id}`} sx={{m:2}}>View Details</Button></Card>}
